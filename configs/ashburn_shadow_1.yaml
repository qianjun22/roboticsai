# ashburn-shadow-1 deployment config
# Fixed: chunk_size drift 8 -> 16 (2026-03-30)
deployment:
  name: ashburn-shadow-1
  region: us-ashburn-1

inference:
  chunk_size: 16          # FIXED: was incorrectly set to 8
  action_dim: 7
  observation_horizon: 2

model:
  checkpoint: finetune_1000_5k/checkpoint-5000
  port: 8001

corrections:
  min_frames: 10
  lift_threshold: 0.78
