"""\ntraining_data_pipeline.py\nOCI Robot Cloud — GR00T N1.6 Fine-Tuning Platform\n\nData ingestion and preprocessing pipeline for robot demonstration data.\nGenerates synthetic demo episodes, filters, splits (stratified by task),\ncomputes stats, and produces an HTML report.\n\nPlatform: OCI A100 GPU4 (138.1.153.110) | GR00T N1.6 3B params\n"""\n\nfrom __future__ import annotations\n\nimport hashlib\nimport json\nimport math\nimport os\nimport random\nimport time\nfrom dataclasses import dataclass, field\nfrom datetime import datetime, timezone\nfrom typing import Dict, List, Optional\n\nimport numpy as np\n\n# ---------------------------------------------------------------------------\n# Constants\n# ---------------------------------------------------------------------------\n\nROBOT_TYPES = [\"franka_panda\", \"ur5\", \"kuka_iiwa\", \"xarm7\"]\nTASK_TYPES  = [\"pick_place\", \"stack\", \"pour\", \"wipe\", \"handover\"]\n\nSPLIT_RATIOS: Dict[str, float] = {\"train\": 0.80, \"val\": 0.10, \"test\": 0.10}\n\nMIN_EPISODE_FRAMES = 10     # drop episodes shorter than this\nSUCCESS_FILTER_RATE = 0.85  # keep batches where >=85 % episodes succeed\n\nREPORT_PATH = \"/tmp/training_data_pipeline.html\"\n\n# ---------------------------------------------------------------------------\n# Dataclasses\n# ---------------------------------------------------------------------------\n\n@dataclass\nclass DemoEpisode:\n    episode_id:  str\n    robot_type:  str\n    task_name:   str\n    n_frames:    int\n    duration_s:  float\n    success:     bool\n    data_path:   str\n    tags:        List[str]\n    created_at:  str  # ISO-8601\n\n\n@dataclass\nclass DatasetSplit:\n    split_name:     str\n    episode_ids:    List[str]\n    n_frames_total: int\n    success_rate:   float\n\n\n@dataclass\nclass PipelineRun:\n    run_id:            str\n    source_episodes:   List[DemoEpisode]\n    output_splits:     Dict[str, DatasetSplit]\n    processing_time_s: float\n    stats:             Dict\n\n\n# ---------------------------------------------------------------------------\n# Data generation\n# ---------------------------------------------------------------------------\n\ndef generate_demo_episodes(n: int = 500, seed: int = 42) -> List[DemoEpisode]:\n    \"\"\"Generate *n* synthetic demo episodes with realistic distributions.\"\"\"\n    rng = random.Random(seed)\n    np_rng = np.random.default_rng(seed)\n\n    robot_weights = [0.40, 0.25, 0.20, 0.15]\n    task_weights  = [0.30, 0.25, 0.15, 0.15, 0.15]\n\n    base_ts = datetime(2026, 1, 1, tzinfo=timezone.utc).timestamp()\n\n    episodes: List[DemoEpisode] = []\n    for i in range(n):\n        robot  = rng.choices(ROBOT_TYPES, weights=robot_weights, k=1)[0]\n        task   = rng.choices(TASK_TYPES,  weights=task_weights,  k=1)[0]\n        raw_frames = int(np_rng.lognormal(mean=4.6, sigma=0.6))\n        n_frames   = max(5, min(raw_frames, 350))\n        fps        = rng.uniform(10.0, 30.0)\n        duration_s = round(n_frames / fps, 2)\n        task_sr = {\"pick_place\": 0.88, \"stack\": 0.82, \"pour\": 0.84, \"wipe\": 0.87, \"handover\": 0.83}\n        success = rng.random() < task_sr[task]\n        ep_id = f\"ep_{i:04d}\"\n        ts    = base_ts + rng.uniform(0, 60 * 24 * 3600)\n        created_at = datetime.fromtimestamp(ts, tz=timezone.utc).strftime(\"%Y-%m-%dT%H:%M:%SZ\")\n        tags = [robot, task]\n        if success: tags.append(\"success\")\n        if n_frames > 200: tags.append(\"long_episode\")\n        episodes.append(DemoEpisode(episode_id=ep_id, robot_type=robot, task_name=task,\n            n_frames=n_frames, duration_s=duration_s, success=success,\n            data_path=f\"/data/demos/{task}/{robot}/{ep_id}.npz\", tags=tags, created_at=created_at))\n    return episodes\n\n\ndef filter_episodes(episodes: List[DemoEpisode]) -> List[DemoEpisode]:\n    after_length  = [e for e in episodes if e.n_frames >= MIN_EPISODE_FRAMES]\n    after_success = [e for e in after_length if e.success]\n    frames = np.array([e.n_frames for e in after_success], dtype=float)\n    q1, q3 = float(np.percentile(frames, 25)), float(np.percentile(frames, 75))\n    upper  = q3 + 3.0 * (q3 - q1)\n    after_outlier = [e for e in after_success if e.n_frames <= upper]\n    if len(episodes) > 0:\n        batch_sr = sum(1 for e in episodes if e.success) / len(episodes)\n        if batch_sr < SUCCESS_FILTER_RATE:\n            print(f\"[WARNING] Batch SR {batch_sr:.2%} below {SUCCESS_FILTER_RATE:.0%}\")\n    return after_outlier\n\n\ndef split_dataset(episodes: List[DemoEpisode], ratios: Dict[str, float] = SPLIT_RATIOS, seed: int = 42) -> Dict[str, DatasetSplit]:\n    rng = random.Random(seed)\n    split_names = list(ratios.keys())\n    buckets: Dict[str, List[str]] = {s: [] for s in split_names}\n    task_groups: Dict[str, List[DemoEpisode]] = {}\n    for ep in episodes:\n        task_groups.setdefault(ep.task_name, []).append(ep)\n    for task, group in task_groups.items():\n        shuffled = group[:]\n        rng.shuffle(shuffled)\n        n = len(shuffled)\n        cut_train = math.floor(n * ratios[\"train\"])\n        cut_val   = cut_train + math.floor(n * ratios[\"val\"])\n        for sname, sl in zip([\"train\",\"val\",\"test\"], [shuffled[:cut_train], shuffled[cut_train:cut_val], shuffled[cut_val:]]):\n            buckets[sname].extend(e.episode_id for e in sl)\n    ep_lookup = {e.episode_id: e for e in episodes}\n    result: Dict[str, DatasetSplit] = {}\n    for sname, ids in buckets.items():\n        eps_in_split = [ep_lookup[eid] for eid in ids]\n        n_frames = sum(e.n_frames for e in eps_in_split)\n        sr = (sum(1 for e in eps_in_split if e.success) / len(eps_in_split) if eps_in_split else 0.0)\n        result[sname] = DatasetSplit(split_name=sname, episode_ids=ids, n_frames_total=n_frames, success_rate=sr)\n    return result\n\n\ndef compute_dataset_stats(splits: Dict[str, DatasetSplit], episodes: List[DemoEpisode]) -> Dict:\n    ep_lookup = {e.episode_id: e for e in episodes}\n    stats: Dict = {}\n    for sname, split in splits.items():\n        eps = [ep_lookup[eid] for eid in split.episode_ids]\n        robot_dist: Dict[str, int] = {}\n        task_dist:  Dict[str, int] = {}\n        for ep in eps:\n            robot_dist[ep.robot_type] = robot_dist.get(ep.robot_type, 0) + 1\n            task_dist[ep.task_name]   = task_dist.get(ep.task_name, 0) + 1\n        frames_arr = np.array([e.n_frames for e in eps], dtype=float)\n        stats[sname] = {\n            \"n_episodes\": len(eps), \"n_frames\": split.n_frames_total,\n            \"success_rate\": round(split.success_rate, 4),\n            \"robot_distribution\": robot_dist, \"task_distribution\": task_dist,\n            \"frames_mean\": round(float(np.mean(frames_arr)), 1) if len(eps) else 0,\n            \"frames_std\":  round(float(np.std(frames_arr)),  1) if len(eps) else 0,\n            \"frames_min\": int(np.min(frames_arr)) if len(eps) else 0,\n            \"frames_max\": int(np.max(frames_arr)) if len(eps) else 0,\n        }\n    return stats\n\n\ndef run_pipeline(n_episodes: int = 500, seed: int = 42) -> PipelineRun:\n    t0 = time.perf_counter()\n    raw_episodes = generate_demo_episodes(n=n_episodes, seed=seed)\n    filtered     = filter_episodes(raw_episodes)\n    splits       = split_dataset(filtered, ratios=SPLIT_RATIOS, seed=seed)\n    stats        = compute_dataset_stats(splits, filtered)\n    stats[\"pipeline\"] = {\n        \"raw_episodes\": len(raw_episodes), \"filtered_episodes\": len(filtered),\n        \"filter_drop_rate\": round(1 - len(filtered) / max(len(raw_episodes), 1), 4),\n        \"total_frames\": sum(s.n_frames_total for s in splits.values()),\n    }\n    run_id = hashlib.sha1(f\"{seed}-{n_episodes}-{time.time()}\".encode()).hexdigest()[:12]\n    return PipelineRun(run_id=run_id, source_episodes=filtered, output_splits=splits,\n                       processing_time_s=round(time.perf_counter() - t0, 4), stats=stats)\n\n\ndef _ascii_bar(value: float, max_value: float, width: int = 20) -> str:\n    filled = int(round(value / max_value * width)) if max_value > 0 else 0\n    return \"\u2588\" * filled + \"\u2591\" * (width - filled)\n\n\ndef _distribution_svg(dist: Dict[str, int], title: str, bar_color: str = \"#C74634\") -> str:\n    if not dist: return \"<p><em>No data</em></p>\"\n    sorted_items = sorted(dist.items(), key=lambda x: -x[1])\n    max_val = max(v for _, v in sorted_items) or 1\n    total   = sum(v for _, v in sorted_items)\n    bar_max_px = 260; row_h = 28; label_w = 120\n    svg_w = label_w + bar_max_px + 60; svg_h = len(sorted_items) * row_h + 30\n    rows = []\n    for idx, (label, count) in enumerate(sorted_items):\n        y = idx * row_h + 20; bar_px = int(count / max_val * bar_max_px); pct = count / total * 100\n        rows.append(f'<text x=\"{label_w-6}\" y=\"{y+14}\" text-anchor=\"end\" font-size=\"11\" fill=\"#cbd5e1\">{label}</text>'\n                    f'<rect x=\"{label_w}\" y=\"{y+2}\" width=\"{bar_px}\" height=\"18\" fill=\"{bar_color}\" rx=\"3\"/>'\n                    f'<text x=\"{label_w+bar_px+6}\" y=\"{y+14}\" font-size=\"11\" fill=\"#94a3b8\">{count} ({pct:.1f}%)</text>')\n    rows_str = \"\\n\".join(rows)\n    return (f'<svg width=\"{svg_w}\" height=\"{svg_h}\" xmlns=\"http://www.w3.org/2000/svg\" style=\"display:block\">'\n            f'<text x=\"0\" y=\"14\" font-size=\"12\" font-weight=\"bold\" fill=\"#e2e8f0\">{title}</text>\\n{rows_str}</svg>')\n\n\ndef generate_pipeline_report(run: PipelineRun) -> str:\n    pipeline_stats = run.stats.get(\"pipeline\", {})\n    split_order = [\"train\", \"val\", \"test\"]\n    table_rows = []\n    for sname in split_order:\n        if sname not in run.output_splits: continue\n        s = run.output_splits[sname]; st = run.stats.get(sname, {})\n        bg = {\"train\": \"#2563eb\", \"val\": \"#d97706\", \"test\": \"#16a34a\"}.get(sname, \"#475569\")\n        table_rows.append(f\"<tr><td><span style='background:{bg};color:#fff;padding:2px 8px;border-radius:4px;font-size:12px'>{sname}</span></td>\"\n                          f\"<td>{st.get('n_episodes',0):,}</td><td>{st.get('n_frames',0):,}</td>\"\n                          f\"<td>{st.get('success_rate',0):.1%}</td>\"\n                          f\"<td>{st.get('frames_mean',0):.0f} \u00b1 {st.get('frames_std',0):.0f}</td>\"\n                          f\"<td>{st.get('frames_min',0)} \u2013 {st.get('frames_max',0)}</td></tr>\")\n    table_rows_html = \"\\n\".join(table_rows)\n    charts_parts = []\n    for sname in split_order:\n        if sname not in run.stats: continue\n        st = run.stats[sname]\n        r_svg = _distribution_svg(st.get(\"robot_distribution\",{}), f\"{sname} \u2014 Robot Types\", \"#C74634\")\n        t_svg = _distribution_svg(st.get(\"task_distribution\", {}), f\"{sname} \u2014 Tasks\",       \"#2563eb\")\n        charts_parts.append(f\"<div style='display:flex;gap:32px;flex-wrap:wrap;margin-bottom:24px'><div>{r_svg}</div><div>{t_svg}</div></div>\")\n    charts_html = \"\\n\".join(charts_parts)\n    generated_at = datetime.now(tz=timezone.utc).strftime(\"%Y-%m-%d %H:%M:%S UTC\")\n    html = f\"\"\"<!DOCTYPE html><html lang=\"en\"><head><meta charset=\"UTF-8\"/>\n<title>OCI Robot Cloud \u2014 Training Data Pipeline</title>\n<style>*{{box-sizing:border-box;margin:0;padding:0}}body{{font-family:-apple-system,BlinkMacSystemFont,\\\"Segoe UI\\\",Roboto,monospace;background:#0f172a;color:#e2e8f0}}\nheader{{background:#1e293b;border-bottom:3px solid #C74634;padding:20px 32px}}\nheader h1{{font-size:22px;color:#f1f5f9;font-weight:700}}header .sub{{font-size:13px;color:#94a3b8;margin-top:4px}}\nmain{{padding:32px;max-width:1100px;margin:0 auto}}\nh2{{font-size:16px;color:#C74634;text-transform:uppercase;letter-spacing:.05em;margin:28px 0 12px}}\n.card{{background:#1e293b;border:1px solid #334155;border-radius:8px;padding:20px;margin-bottom:20px}}\n.kpi-row{{display:flex;gap:16px;flex-wrap:wrap;margin-bottom:24px}}\n.kpi{{background:#1e293b;border:1px solid #334155;border-radius:8px;padding:16px 20px;flex:1;min-width:160px}}\n.kpi .label{{font-size:11px;color:#94a3b8;text-transform:uppercase;letter-spacing:.06em;margin-bottom:6px}}\n.kpi .value{{font-size:26px;font-weight:700;color:#f1f5f9}}\ntable{{width:100%;border-collapse:collapse;font-size:13px}}\nth{{background:#0f172a;color:#94a3b8;text-transform:uppercase;font-size:11px;letter-spacing:.06em;padding:10px 12px;text-align:left;border-bottom:1px solid #334155}}\ntd{{padding:10px 12px;border-bottom:1px solid #1e293b;color:#cbd5e1}}\ntr:hover td{{background:#1e293b}}\nfooter{{font-size:11px;color:#475569;text-align:center;padding:24px 0 32px}}</style></head>\n<body><header><div><h1>OCI Robot Cloud \u2014 Training Data Pipeline</h1>\n<div class=\"sub\">GR00T N1.6 (3B) &nbsp;|&nbsp; OCI A100 GPU4 138.1.153.110 &nbsp;|&nbsp; Run: {run.run_id} &nbsp;|&nbsp; {generated_at}</div></div></header>\n<main>\n<h2>Pipeline Summary</h2><div class=\"kpi-row\">\n<div class=\"kpi\"><div class=\"label\">Raw Episodes</div><div class=\"value\">{pipeline_stats.get('raw_episodes',0):,}</div></div>\n<div class=\"kpi\"><div class=\"label\">After Filtering</div><div class=\"value\">{pipeline_stats.get('filtered_episodes',0):,}</div></div>\n<div class=\"kpi\"><div class=\"label\">Drop Rate</div><div class=\"value\">{pipeline_stats.get('filter_drop_rate',0):.1%}</div></div>\n<div class=\"kpi\"><div class=\"label\">Total Frames</div><div class=\"value\">{pipeline_stats.get('total_frames',0):,}</div></div>\n<div class=\"kpi\"><div class=\"label\">Processing Time</div><div class=\"value\">{run.processing_time_s:.3f}s</div></div>\n</div>\n<h2>Dataset Splits</h2><div class=\"card\" style=\"padding:0\"><table>\n<thead><tr><th>Split</th><th>Episodes</th><th>Frames</th><th>Success Rate</th><th>Frames Mean\u00b1SD</th><th>Range</th></tr></thead>\n<tbody>{table_rows_html}</tbody></table></div>\n<h2>Distributions</h2>{charts_html}\n</main><footer>Oracle Confidential &nbsp;|&nbsp; Training Data Pipeline &nbsp;|&nbsp; {generated_at}</footer>\n</body></html>\"\"\"\n    with open(REPORT_PATH, \"w\", encoding=\"utf-8\") as fh:\n        fh.write(html)\n    return REPORT_PATH\n\n\ndef main() -> None:\n    print(\"=\" * 65)\n    print(\"  OCI Robot Cloud \u2014 Training Data Pipeline\")\n    print(\"  GR00T N1.6 (3B params) | OCI A100 GPU4 138.1.153.110\")\n    print(\"=\" * 65)\n    run = run_pipeline(n_episodes=500, seed=42)\n    ps = run.stats[\"pipeline\"]\n    print(f\"\\n  Raw: {ps['raw_episodes']}  Filtered: {ps['filtered_episodes']}  Drop: {ps['filter_drop_rate']:.1%}  Frames: {ps['total_frames']:,}\")\n    for sname in [\"train\",\"val\",\"test\"]:\n        if sname in run.stats:\n            st = run.stats[sname]\n            print(f\"  {sname:<6} {st['n_episodes']:>4} eps  {st['n_frames']:>7,} frames  SR={st['success_rate']:.2%}  mean={st['frames_mean']:.1f}\u00b1{st['frames_std']:.1f}\")\n    report_path = generate_pipeline_report(run)\n    print(f\"\\n  Report: {report_path}\")\n    print(\"=\" * 65)\n\n\nif __name__ == \"__main__\":\n    main()\n