"""\nPartner Self-Service Portal — OCI Robot Cloud\nPort 8079\n\nLets design partners submit training jobs, check status, and download results\nwithout needing OCI console access.\n"""\n\nfrom __future__ import annotations\nimport uuid\nfrom datetime import datetime, timedelta, timezone\nfrom enum import Enum\nfrom typing import List, Optional\nfrom fastapi import FastAPI, HTTPException, Query\nfrom fastapi.responses import HTMLResponse\nfrom pydantic import BaseModel, Field\n\nclass JobStatus(str, Enum):\n    queued = \"queued\"\n    running = \"running\"\n    done = \"done\"\n    failed = \"failed\"\n    cancelled = \"cancelled\"\n\nclass UploadStatus(str, Enum):\n    uploading = \"uploading\"\n    processing = \"processing\"\n    ready = \"ready\"\n    failed = \"failed\"\n\nclass TrainingJob(BaseModel):\n    id: str\n    partner_id: str\n    job_name: str\n    dataset_path: str\n    base_model: str\n    lora_rank: int = 16\n    training_steps: int\n    status: JobStatus\n    submitted_at: datetime\n    started_at: Optional[datetime] = None\n    completed_at: Optional[datetime] = None\n    mae: Optional[float] = None\n    success_rate: Optional[float] = None\n    cost_usd: Optional[float] = None\n    gpu_hours: Optional[float] = None\n    checkpoint_url: Optional[str] = None\n    error_msg: Optional[str] = None\n\nclass DatasetUpload(BaseModel):\n    id: str\n    partner_id: str\n    filename: str\n    size_bytes: int\n    demo_count: int\n    upload_status: UploadStatus\n    created_at: datetime\n\nclass APIKey(BaseModel):\n    partner_id: str\n    key_hash: str\n    created_at: datetime\n    last_used: datetime\n    active: bool\n\nclass JobSubmitRequest(BaseModel):\n    partner_id: str\n    job_name: str\n    dataset_path: str\n    base_model: str = \"gr00t-n1.6\"\n    lora_rank: int = Field(default=16, ge=4, le=64)\n    training_steps: int = Field(default=2000, ge=100, le=50000)\n\nclass DatasetRegisterRequest(BaseModel):\n    partner_id: str\n    filename: str\n    size_bytes: int\n    demo_count: int\n\ndef _now(): return datetime.now(tz=timezone.utc)\ndef _dt(days_ago=0, hours_ago=0): return _now() - timedelta(days=days_ago, hours=hours_ago)\n\nGPU_RATE = 4.10\nMONTHLY_QUOTA_GPU_HRS = 200.0\n\nAPI_KEYS = {\n    \"covariant\": APIKey(partner_id=\"covariant\", key_hash=\"ck_live_a3f8b2d1***\",\n        created_at=_dt(days_ago=90), last_used=_dt(hours_ago=2), active=True),\n    \"apptronik\": APIKey(partner_id=\"apptronik\", key_hash=\"ak_live_7e2c9f04***\",\n        created_at=_dt(days_ago=75), last_used=_dt(days_ago=2), active=True),\n    \"1x-technologies\": APIKey(partner_id=\"1x-technologies\", key_hash=\"ox_live_5d1a8c33***\",\n        created_at=_dt(days_ago=60), last_used=_dt(days_ago=1), active=True),\n}\n\nJOBS = {\n    \"job-cov-001\": TrainingJob(id=\"job-cov-001\", partner_id=\"covariant\",\n        job_name=\"pick-place-v3-finetune\", dataset_path=\"oci://cov-datasets/pick_place_v3/\",\n        base_model=\"gr00t-n1.6\", lora_rank=16, training_steps=5000, status=JobStatus.done,\n        submitted_at=_dt(days_ago=25), started_at=_dt(days_ago=25)+timedelta(minutes=7),\n        completed_at=_dt(days_ago=25)+timedelta(hours=3,minutes=22),\n        mae=0.017, success_rate=0.71, cost_usd=14.27, gpu_hours=3.48,\n        checkpoint_url=\"oci://cov-checkpoints/pick-place-v3-finetune/ckpt_5000.pt\"),\n    \"job-cov-002\": TrainingJob(id=\"job-cov-002\", partner_id=\"covariant\",\n        job_name=\"bin-sorting-v2-finetune\", dataset_path=\"oci://cov-datasets/bin_sorting_v2/\",\n        base_model=\"gr00t-n1.6\", lora_rank=16, training_steps=4000, status=JobStatus.done,\n        submitted_at=_dt(days_ago=18), started_at=_dt(days_ago=18)+timedelta(minutes=9),\n        completed_at=_dt(days_ago=18)+timedelta(hours=2,minutes=41),\n        mae=0.019, success_rate=0.68, cost_usd=11.48, gpu_hours=2.80,\n        checkpoint_url=\"oci://cov-checkpoints/bin-sorting-v2-finetune/ckpt_4000.pt\"),\n    \"job-cov-003\": TrainingJob(id=\"job-cov-003\", partner_id=\"covariant\",\n        job_name=\"pallet-stack-v1-finetune\", dataset_path=\"oci://cov-datasets/pallet_stack_v1/\",\n        base_model=\"gr00t-n1.6\", lora_rank=32, training_steps=6000, status=JobStatus.running,\n        submitted_at=_dt(hours_ago=5), started_at=_dt(hours_ago=4)),\n    \"job-apt-001\": TrainingJob(id=\"job-apt-001\", partner_id=\"apptronik\",\n        job_name=\"apollo-loco-manipulation\", dataset_path=\"oci://apt-datasets/loco_manip_v1/\",\n        base_model=\"gr00t-n1.6\", lora_rank=16, training_steps=3500, status=JobStatus.done,\n        submitted_at=_dt(days_ago=14), started_at=_dt(days_ago=14)+timedelta(minutes=6),\n        completed_at=_dt(days_ago=14)+timedelta(hours=2,minutes=18),\n        mae=0.024, success_rate=0.58, cost_usd=9.57, gpu_hours=2.33,\n        checkpoint_url=\"oci://apt-checkpoints/apollo-loco-manipulation/ckpt_3500.pt\"),\n    \"job-apt-002\": TrainingJob(id=\"job-apt-002\", partner_id=\"apptronik\",\n        job_name=\"apollo-door-open-v1\", dataset_path=\"oci://apt-datasets/door_open_v1/\",\n        base_model=\"gr00t-n1.6\", lora_rank=16, training_steps=2500, status=JobStatus.queued,\n        submitted_at=_dt(hours_ago=1)),\n    \"job-1x-001\": TrainingJob(id=\"job-1x-001\", partner_id=\"1x-technologies\",\n        job_name=\"neo-assembly-v2\", dataset_path=\"oci://1x-datasets/assembly_v2/\",\n        base_model=\"gr00t-n1.6\", lora_rank=16, training_steps=4500, status=JobStatus.done,\n        submitted_at=_dt(days_ago=10), started_at=_dt(days_ago=10)+timedelta(minutes=8),\n        completed_at=_dt(days_ago=10)+timedelta(hours=3,minutes=5),\n        mae=0.021, success_rate=0.63, cost_usd=12.73, gpu_hours=3.10,\n        checkpoint_url=\"oci://1x-checkpoints/neo-assembly-v2/ckpt_4500.pt\"),\n    \"job-1x-002\": TrainingJob(id=\"job-1x-002\", partner_id=\"1x-technologies\",\n        job_name=\"neo-shelf-restock-v1\", dataset_path=\"oci://1x-datasets/shelf_restock_v1/\",\n        base_model=\"gr00t-n1.6\", lora_rank=16, training_steps=3000, status=JobStatus.running,\n        submitted_at=_dt(hours_ago=3), started_at=_dt(hours_ago=2)),\n    \"job-skl-001\": TrainingJob(id=\"job-skl-001\", partner_id=\"skild-ai\",\n        job_name=\"generalist-policy-v1\", dataset_path=\"oci://skl-datasets/generalist_v1/\",\n        base_model=\"gr00t-n1.6\", lora_rank=16, training_steps=10000, status=JobStatus.failed,\n        submitted_at=_dt(days_ago=5), started_at=_dt(days_ago=5)+timedelta(minutes=11),\n        completed_at=_dt(days_ago=5)+timedelta(minutes=34),\n        error_msg=\"Insufficient demo quality: 47% rejected (threshold: <30%)\"),\n}\n\nDATASETS = {\n    \"ds-cov-001\": DatasetUpload(id=\"ds-cov-001\", partner_id=\"covariant\",\n        filename=\"pick_place_v3_500demos.tar.gz\", size_bytes=int(2.1*1024**3),\n        demo_count=500, upload_status=UploadStatus.ready, created_at=_dt(days_ago=26)),\n    \"ds-apt-001\": DatasetUpload(id=\"ds-apt-001\", partner_id=\"apptronik\",\n        filename=\"loco_manip_v1_280demos.tar.gz\", size_bytes=int(1.4*1024**3),\n        demo_count=280, upload_status=UploadStatus.ready, created_at=_dt(days_ago=15)),\n    \"ds-1x-001\": DatasetUpload(id=\"ds-1x-001\", partner_id=\"1x-technologies\",\n        filename=\"assembly_v2_560demos.tar.gz\", size_bytes=int(2.8*1024**3),\n        demo_count=560, upload_status=UploadStatus.ready, created_at=_dt(days_ago=11)),\n}\n\napp = FastAPI(title=\"OCI Robot Cloud — Partner Self-Service Portal\", version=\"1.0.0\")\n\ndef _partner_gpu_hours(partner_id):\n    now = _now()\n    start = now.replace(day=1, hour=0, minute=0, second=0, microsecond=0)\n    total = 0.0\n    for job in JOBS.values():\n        if job.partner_id != partner_id or job.submitted_at < start: continue\n        if job.gpu_hours: total += job.gpu_hours\n        elif job.status == JobStatus.running and job.started_at:\n            total += (now - job.started_at).total_seconds() / 3600\n    return round(total, 2)\n\n@app.get(\"/\", response_class=HTMLResponse)\ndef dashboard():\n    running = sum(1 for j in JOBS.values() if j.status==JobStatus.running)\n    done = sum(1 for j in JOBS.values() if j.status==JobStatus.done)\n    total_gpu = sum(j.gpu_hours or 0 for j in JOBS.values())\n    rows = \"\".join(\n        f\"<tr><td>{j.job_name}</td><td>{j.partner_id}</td><td>{j.status.value}</td>\"\n        f\"<td>{j.mae or '—'}</td><td>{j.success_rate or '—'}</td></tr>\"\n        for j in sorted(JOBS.values(), key=lambda x: x.submitted_at, reverse=True)\n    )\n    return f\"\"\"<!DOCTYPE html><html><head><title>Partner Portal</title></head><body>\n    <h1>OCI Robot Cloud — Partner Self-Service Portal (Port 8079)</h1>\n    <p>Running: {running} | Done: {done} | Total GPU-hrs: {total_gpu:.1f}</p>\n    <table border=1><tr><th>Job</th><th>Partner</th><th>Status</th><th>MAE</th><th>SR</th></tr>\n    {rows}</table>\n    <p><a href='/docs'>API Docs</a></p></body></html>\"\"\"\n\n@app.get(\"/health\")\ndef health(): return {\"status\": \"ok\"}\n\n@app.get(\"/api/jobs\")\ndef list_jobs(partner_id: Optional[str] = Query(default=None)):\n    jobs = [j for j in JOBS.values() if not partner_id or j.partner_id==partner_id]\n    return {\"jobs\": sorted(jobs, key=lambda j: j.submitted_at, reverse=True), \"count\": len(jobs)}\n\n@app.post(\"/api/jobs\", status_code=201)\ndef submit_job(req: JobSubmitRequest):\n    job_id = f\"job-{uuid.uuid4().hex[:8]}\"\n    job = TrainingJob(id=job_id, partner_id=req.partner_id, job_name=req.job_name,\n        dataset_path=req.dataset_path, base_model=req.base_model, lora_rank=req.lora_rank,\n        training_steps=req.training_steps, status=JobStatus.queued, submitted_at=_now())\n    JOBS[job_id] = job\n    return {\"job_id\": job_id, \"status\": \"queued\"}\n\n@app.get(\"/api/jobs/{job_id}\")\ndef get_job(job_id: str):\n    job = JOBS.get(job_id)\n    if not job: raise HTTPException(status_code=404, detail=f\"Job '{job_id}' not found.\")\n    return job\n\n@app.delete(\"/api/jobs/{job_id}\")\ndef cancel_job(job_id: str):\n    job = JOBS.get(job_id)\n    if not job: raise HTTPException(status_code=404, detail=f\"Job '{job_id}' not found.\")\n    if job.status != JobStatus.queued:\n        raise HTTPException(status_code=409, detail=\"Only queued jobs can be cancelled.\")\n    job.status = JobStatus.cancelled\n    return {\"job_id\": job_id, \"status\": \"cancelled\"}\n\n@app.get(\"/api/datasets\")\ndef list_datasets(partner_id: Optional[str] = Query(default=None)):\n    ds = [d for d in DATASETS.values() if not partner_id or d.partner_id==partner_id]\n    return {\"datasets\": ds, \"count\": len(ds)}\n\n@app.get(\"/api/usage\")\ndef get_usage(partner_id: str = Query(...)):\n    used = _partner_gpu_hours(partner_id)\n    cost = round(used * GPU_RATE, 2)\n    return {\"partner_id\": partner_id, \"gpu_hours_used\": used,\n            \"gpu_hours_quota\": MONTHLY_QUOTA_GPU_HRS, \"cost_usd\": cost, \"gpu_rate\": GPU_RATE}\n\nif __name__ == \"__main__\":\n    import uvicorn\n    uvicorn.run(\"partner_self_service_portal:app\", host=\"0.0.0.0\", port=8079, reload=True)\n