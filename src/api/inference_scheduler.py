"""\ninference_scheduler.py\nOCI Robot Cloud — GR00T N1.6 batch inference queue manager.\n\nManages concurrent inference requests from multiple design partners,\nenforces per-partner rate limits, and batches requests for GPU efficiency.\n\nUsage:\n    pip install fastapi uvicorn pydantic\n    python src/api/inference_scheduler.py\n\nPort: 8078\n"""\n\nfrom __future__ import annotations\n\nimport math\nimport random\nimport uuid\nfrom datetime import datetime, timezone, timedelta\nfrom typing import Optional\n\nfrom fastapi import FastAPI, HTTPException\nfrom fastapi.responses import HTMLResponse\nfrom pydantic import BaseModel, Field\nimport uvicorn\n\n# ---------------------------------------------------------------------------\n# Constants\n# ---------------------------------------------------------------------------\nPORT = 8078\nA100_LATENCY_MS_AVG = 226.0\nA100_LATENCY_JITTER_MS = 15.0\nCOST_PER_REQUEST_USD = 0.00114   # $4.10/hr / (3600s/hr / 1.5s per request)\n\n# ---------------------------------------------------------------------------\n# Data models\n# ---------------------------------------------------------------------------\n\nclass InferenceRequest(BaseModel):\n    id: str\n    partner_id: str\n    robot_type: str\n    observation_image_b64: str   # simulated base64 payload\n    priority: int = Field(ge=1, le=5)\n    submitted_at: datetime\n    status: str   # queued | running | done | failed\n    result_latency_ms: Optional[float] = None\n    chunk_id: int = 0\n    error_msg: Optional[str] = None\n\n\nclass BatchResult(BaseModel):\n    batch_id: str\n    requests_processed: int\n    avg_latency_ms: float\n    total_cost_usd: float\n    completed_at: datetime\n\n\nclass PartnerQuota(BaseModel):\n    partner_id: str\n    requests_per_minute_limit: int\n    burst_limit: int\n    used_this_minute: int\n    queue_depth: int\n\n\nclass SubmitRequest(BaseModel):\n    partner_id: str\n    robot_type: str = \"UR5e\"\n    observation_image_b64: str = \"simulated_obs_b64\"\n    priority: int = Field(default=3, ge=1, le=5)\n    chunk_id: int = 0\n\n# ---------------------------------------------------------------------------\n# Seeded state\n# ---------------------------------------------------------------------------\n\ndef _now(offset_seconds: int = 0) -> datetime:\n    return datetime.now(timezone.utc) + timedelta(seconds=offset_seconds)\n\n\ndef _jitter_latency() -> float:\n    return A100_LATENCY_MS_AVG + (random.uniform(-1.0, 1.0) * A100_LATENCY_JITTER_MS)\n\n\n_PARTNER_CONFIGS = [\n    (\"covariant\",             60, 80),\n    (\"apptronik\",             30, 40),\n    (\"skild-ai\",              20, 30),\n    (\"1x-technologies\",       25, 35),\n    (\"physical-intelligence\", 10, 15),\n]\n\npartners: dict[str, PartnerQuota] = {}\nfor _pid, _rpm, _burst in _PARTNER_CONFIGS:\n    partners[_pid] = PartnerQuota(\n        partner_id=_pid,\n        requests_per_minute_limit=_rpm,\n        burst_limit=_burst,\n        used_this_minute=0,\n        queue_depth=0,\n    )\n\nrequests_db: dict[str, InferenceRequest] = {}\n\ndef _seed_request(partner, status, priority=3, chunk_id=0, offset_secs=-300,\n                  latency=None, robot=\"UR5e\", error=None):\n    req = InferenceRequest(\n        id=str(uuid.uuid4()),\n        partner_id=partner,\n        robot_type=robot,\n        observation_image_b64=\"simulated_frame_b64_placeholder\",\n        priority=priority,\n        submitted_at=_now(offset_secs),\n        status=status,\n        result_latency_ms=latency,\n        chunk_id=chunk_id,\n        error_msg=error,\n    )\n    requests_db[req.id] = req\n    return req\n\n_seed_request(\"covariant\", \"done\", priority=4, chunk_id=0, offset_secs=-600, latency=218.4)\n_seed_request(\"covariant\", \"done\", priority=4, chunk_id=1, offset_secs=-580, latency=231.7)\n_seed_request(\"covariant\", \"done\", priority=3, chunk_id=2, offset_secs=-560, latency=224.1)\n_seed_request(\"apptronik\", \"done\", priority=3, chunk_id=0, offset_secs=-540, latency=229.9)\n_seed_request(\"apptronik\", \"done\", priority=2, chunk_id=1, offset_secs=-520, latency=237.2, robot=\"Apptronik Apollo\")\n_seed_request(\"covariant\", \"queued\", priority=5, chunk_id=3, offset_secs=-30)\n_seed_request(\"covariant\", \"queued\", priority=4, chunk_id=4, offset_secs=-28)\n_seed_request(\"apptronik\", \"queued\", priority=3, chunk_id=2, offset_secs=-25, robot=\"Apptronik Apollo\")\n_seed_request(\"apptronik\", \"queued\", priority=3, chunk_id=3, offset_secs=-22, robot=\"Apptronik Apollo\")\n_seed_request(\"skild-ai\", \"queued\", priority=2, chunk_id=0, offset_secs=-20, robot=\"Franka FR3\")\n_seed_request(\"1x-technologies\", \"queued\", priority=3, chunk_id=1, offset_secs=-18, robot=\"1X NEO\")\n_seed_request(\"physical-intelligence\", \"queued\", priority=1, chunk_id=0, offset_secs=-15)\n_seed_request(\"physical-intelligence\", \"queued\", priority=2, chunk_id=1, offset_secs=-12)\n_seed_request(\"covariant\", \"running\", priority=5, chunk_id=5, offset_secs=-5)\n_seed_request(\"covariant\", \"running\", priority=4, chunk_id=6, offset_secs=-4)\n_seed_request(\"covariant\", \"running\", priority=4, chunk_id=7, offset_secs=-3)\n_seed_request(\"1x-technologies\", \"running\", priority=3, chunk_id=2, offset_secs=-6, robot=\"1X NEO\")\n_seed_request(\"1x-technologies\", \"running\", priority=3, chunk_id=3, offset_secs=-5, robot=\"1X NEO\")\n_seed_request(\"skild-ai\", \"failed\", priority=2, chunk_id=0, offset_secs=-400, robot=\"Franka FR3\",\n              error=\"CUDA OOM: activation cache exceeded 4GB\")\n_seed_request(\"skild-ai\", \"failed\", priority=2, chunk_id=1, offset_secs=-395, robot=\"Franka FR3\",\n              error=\"Observation tensor shape mismatch: expected (1,3,224,224) got (1,3,256,256)\")\n\nfor _req in requests_db.values():\n    if _req.status in (\"queued\", \"running\"):\n        partners[_req.partner_id].queue_depth += 1\n    if _req.status == \"done\":\n        partners[_req.partner_id].used_this_minute = max(0, partners[_req.partner_id].used_this_minute + 1)\n\nbatches_db: list[BatchResult] = [\n    BatchResult(batch_id=str(uuid.uuid4()), requests_processed=12, avg_latency_ms=223.6,\n                total_cost_usd=12*COST_PER_REQUEST_USD, completed_at=_now(-900)),\n    BatchResult(batch_id=str(uuid.uuid4()), requests_processed=8, avg_latency_ms=228.4,\n                total_cost_usd=8*COST_PER_REQUEST_USD, completed_at=_now(-720)),\n    BatchResult(batch_id=str(uuid.uuid4()), requests_processed=15, avg_latency_ms=221.9,\n                total_cost_usd=15*COST_PER_REQUEST_USD, completed_at=_now(-540)),\n    BatchResult(batch_id=str(uuid.uuid4()), requests_processed=10, avg_latency_ms=234.7,\n                total_cost_usd=10*COST_PER_REQUEST_USD, completed_at=_now(-360)),\n    BatchResult(batch_id=str(uuid.uuid4()), requests_processed=7, avg_latency_ms=225.3,\n                total_cost_usd=7*COST_PER_REQUEST_USD, completed_at=_now(-180)),\n]\n\napp = FastAPI(title=\"OCI Robot Cloud — Inference Scheduler\", version=\"1.0.0\")\n\n@app.get(\"/\", response_class=HTMLResponse)\nasync def dashboard():\n    total_queued = sum(1 for r in requests_db.values() if r.status == \"queued\")\n    total_running = sum(1 for r in requests_db.values() if r.status == \"running\")\n    total_done = sum(1 for r in requests_db.values() if r.status == \"done\")\n    total_failed = sum(1 for r in requests_db.values() if r.status == \"failed\")\n    done_latencies = [r.result_latency_ms for r in requests_db.values() if r.result_latency_ms]\n    avg_lat = sum(done_latencies)/len(done_latencies) if done_latencies else A100_LATENCY_MS_AVG\n    cost_today = total_done * COST_PER_REQUEST_USD\n    return f\"\"\"<!DOCTYPE html><html><head><title>Inference Scheduler</title></head><body>\n    <h1>OCI Robot Cloud — Inference Scheduler (Port {PORT})</h1>\n    <p>Queued: {total_queued} | Running: {total_running} | Done: {total_done} | Failed: {total_failed}</p>\n    <p>Avg Latency: {avg_lat:.1f}ms | Cost Today: ${cost_today:.4f}</p>\n    <p>Partners: {len(partners)} | Batches: {len(batches_db)}</p>\n    <p><a href='/docs'>API Docs</a> | <a href='/api/stats'>Stats</a></p>\n    </body></html>\"\"\"\n\n@app.get(\"/api/queue\")\nasync def get_queue():\n    active = [r for r in requests_db.values() if r.status in (\"queued\", \"running\")]\n    active_sorted = sorted(active, key=lambda r: (-r.priority, r.submitted_at))\n    return {\"queued\": [r for r in active_sorted if r.status==\"queued\"],\n            \"running\": [r for r in active_sorted if r.status==\"running\"],\n            \"total_active\": len(active_sorted)}\n\n@app.post(\"/api/submit\", status_code=201)\nasync def submit_request(body: SubmitRequest):\n    partner_id = body.partner_id\n    if partner_id not in partners:\n        raise HTTPException(status_code=404, detail=f\"Unknown partner '{partner_id}'\")\n    quota = partners[partner_id]\n    if quota.used_this_minute >= quota.requests_per_minute_limit:\n        raise HTTPException(status_code=429, detail={\"error\": \"rate_limit_exceeded\", \"partner_id\": partner_id})\n    if quota.queue_depth >= quota.burst_limit:\n        raise HTTPException(status_code=429, detail={\"error\": \"burst_limit_exceeded\", \"partner_id\": partner_id})\n    req = InferenceRequest(id=str(uuid.uuid4()), partner_id=partner_id, robot_type=body.robot_type,\n        observation_image_b64=body.observation_image_b64, priority=body.priority,\n        submitted_at=datetime.now(timezone.utc), status=\"queued\", chunk_id=body.chunk_id)\n    requests_db[req.id] = req\n    quota.used_this_minute += 1\n    quota.queue_depth += 1\n    return {\"request_id\": req.id, \"status\": \"queued\", \"cost_usd\": COST_PER_REQUEST_USD,\n            \"estimated_latency_ms\": A100_LATENCY_MS_AVG}\n\n@app.get(\"/api/request/{request_id}\")\nasync def get_request(request_id: str):\n    req = requests_db.get(request_id)\n    if not req:\n        raise HTTPException(status_code=404, detail=f\"Request '{request_id}' not found.\")\n    return req\n\n@app.get(\"/api/quota/{partner_id}\")\nasync def get_quota(partner_id: str):\n    quota = partners.get(partner_id)\n    if not quota:\n        raise HTTPException(status_code=404, detail=f\"Unknown partner '{partner_id}'\")\n    return {**quota.model_dump(),\n            \"utilisation_pct\": round(quota.used_this_minute/quota.requests_per_minute_limit*100, 1),\n            \"remaining_this_minute\": quota.requests_per_minute_limit - quota.used_this_minute}\n\n@app.get(\"/api/batches\")\nasync def list_batches():\n    return {\"batches\": list(reversed(batches_db)), \"total\": len(batches_db),\n            \"total_cost_usd\": sum(b.total_cost_usd for b in batches_db)}\n\n@app.get(\"/api/stats\")\nasync def get_stats():\n    done_reqs = [r for r in requests_db.values() if r.status==\"done\"]\n    done_latencies = [r.result_latency_ms for r in done_reqs if r.result_latency_ms]\n    avg_lat = sum(done_latencies)/len(done_latencies) if done_latencies else A100_LATENCY_MS_AVG\n    return {\"summary\": {\"total_requests_today\": len(requests_db), \"done\": len(done_reqs),\n            \"avg_latency_ms\": round(avg_lat,2), \"cost_today_usd\": round(len(done_reqs)*COST_PER_REQUEST_USD,5),\n            \"model\": \"GR00T N1.6\", \"hardware\": \"A100 80GB\"}}\n\n@app.get(\"/health\")\nasync def health():\n    return {\"status\": \"ok\", \"port\": PORT, \"model\": \"GR00T N1.6\"}\n\nif __name__ == \"__main__\":\n    print(f\"OCI Robot Cloud — Inference Scheduler | Port {PORT} | {len(partners)} partners\")\n    uvicorn.run(app, host=\"0.0.0.0\", port=PORT)\n