"""\nSLA Reporting Service — OCI Robot Cloud\nPort 8080\n\nTracks SLA commitments to design partners and generates monthly compliance reports.\nAll 7 metrics PASSING for March 2026.\n"""\n\nfrom __future__ import annotations\nfrom datetime import datetime, timezone\nfrom enum import Enum\nfrom typing import List, Optional\nfrom fastapi import FastAPI, HTTPException, Query\nfrom fastapi.responses import HTMLResponse\nfrom pydantic import BaseModel\n\nclass SLAStatus(str, Enum):\n    pass_ = \"pass\"\n    warn = \"warn\"\n    fail = \"fail\"\n\nclass Severity(str, Enum):\n    P1 = \"P1\"\n    P2 = \"P2\"\n    P3 = \"P3\"\n\nclass Trend(str, Enum):\n    improving = \"improving\"\n    stable = \"stable\"\n    degrading = \"degrading\"\n\nclass SLAMetric(BaseModel):\n    metric_name: str\n    sla_target: str\n    current_value: str\n    unit: str\n    status: SLAStatus\n    measurement_period: str\n    trend: Trend\n    target_value: float\n    actual_value: float\n    lower_is_better: bool = True\n\nclass IncidentRecord(BaseModel):\n    id: str\n    severity: Severity\n    title: str\n    start_time: datetime\n    end_time: datetime\n    duration_minutes: int\n    affected_partners: List[str]\n    root_cause: str\n    resolution: str\n    credits_owed_usd: float = 0.0\n\nclass PartnerSLAReport(BaseModel):\n    partner_id: str\n    report_month: str\n    sla_metrics: List[SLAMetric]\n    incidents_count: int\n    uptime_pct: float\n    credits_owed_usd: float\n\ndef _utc(y, m, d, h=0, mi=0): return datetime(y, m, d, h, mi, tzinfo=timezone.utc)\n\nMEASUREMENT_PERIOD = \"March 2026\"\n\nSLA_METRICS = [\n    SLAMetric(metric_name=\"Inference Latency p99\", sla_target=\"< 300 ms\",\n        current_value=\"267 ms\", unit=\"ms\", status=SLAStatus.pass_,\n        measurement_period=MEASUREMENT_PERIOD, trend=Trend.improving,\n        target_value=300.0, actual_value=267.0, lower_is_better=True),\n    SLAMetric(metric_name=\"Inference Latency p50\", sla_target=\"< 250 ms\",\n        current_value=\"226 ms\", unit=\"ms\", status=SLAStatus.pass_,\n        measurement_period=MEASUREMENT_PERIOD, trend=Trend.stable,\n        target_value=250.0, actual_value=226.0, lower_is_better=True),\n    SLAMetric(metric_name=\"API Availability\", sla_target=\"> 99.5%\",\n        current_value=\"99.94%\", unit=\"%\", status=SLAStatus.pass_,\n        measurement_period=MEASUREMENT_PERIOD, trend=Trend.stable,\n        target_value=99.5, actual_value=99.94, lower_is_better=False),\n    SLAMetric(metric_name=\"Training Job Start Time (avg)\", sla_target=\"< 15 min\",\n        current_value=\"8.2 min\", unit=\"min\", status=SLAStatus.pass_,\n        measurement_period=MEASUREMENT_PERIOD, trend=Trend.improving,\n        target_value=15.0, actual_value=8.2, lower_is_better=True),\n    SLAMetric(metric_name=\"Training Completion On-Time Rate\", sla_target=\"> 90%\",\n        current_value=\"94%\", unit=\"%\", status=SLAStatus.pass_,\n        measurement_period=MEASUREMENT_PERIOD, trend=Trend.stable,\n        target_value=90.0, actual_value=94.0, lower_is_better=False),\n    SLAMetric(metric_name=\"Data Retention\", sla_target=\">= 90 days\",\n        current_value=\"365 days\", unit=\"days\", status=SLAStatus.pass_,\n        measurement_period=MEASUREMENT_PERIOD, trend=Trend.stable,\n        target_value=90.0, actual_value=365.0, lower_is_better=False),\n    SLAMetric(metric_name=\"P1 Support Response Time (avg)\", sla_target=\"< 4 hours\",\n        current_value=\"1.8 hours\", unit=\"hours\", status=SLAStatus.pass_,\n        measurement_period=MEASUREMENT_PERIOD, trend=Trend.improving,\n        target_value=4.0, actual_value=1.8, lower_is_better=True),\n]\n\nINCIDENTS = [\n    IncidentRecord(id=\"INC-2026-031\", severity=Severity.P2,\n        title=\"Ashburn region degraded latency\",\n        start_time=_utc(2026,3,15,14,12), end_time=_utc(2026,3,15,14,57),\n        duration_minutes=45, affected_partners=[\"covariant\",\"apptronik\"],\n        root_cause=\"NVLink timeout on 2 of 16 A100 nodes. Latency p99 spiked to ~820ms.\",\n        resolution=\"Nodes drained and restarted; NVLink firmware patched. Under 1-hour threshold.\",\n        credits_owed_usd=0.0),\n    IncidentRecord(id=\"INC-2026-032\", severity=Severity.P3,\n        title=\"Training queue backup\",\n        start_time=_utc(2026,3,21,9,4), end_time=_utc(2026,3,21,9,26),\n        duration_minutes=22, affected_partners=[\"covariant\",\"apptronik\",\"1x-technologies\",\"skild-ai\"],\n        root_cause=\"OCI spot reclaim on 4 A100 nodes during maintenance. 3 jobs re-queued.\",\n        resolution=\"On-demand fallback activated. All jobs resumed within SLA window.\",\n        credits_owed_usd=0.0),\n    IncidentRecord(id=\"INC-2026-033\", severity=Severity.P2,\n        title=\"Inference API 503 errors\",\n        start_time=_utc(2026,3,27,22,41), end_time=_utc(2026,3,27,22,49),\n        duration_minutes=8, affected_partners=[\"1x-technologies\"],\n        root_cause=\"OOM kill on inference worker pod (GR00T N1.6, 6.7GB) with 12 concurrent requests.\",\n        resolution=\"Pod restarted in 3min; memory limit raised 10GB→14GB. Under 30-min threshold.\",\n        credits_owed_usd=0.0),\n]\n\nALL_PARTNERS = [\"covariant\", \"apptronik\", \"1x-technologies\", \"skild-ai\"]\n\nPARTNER_REPORTS = []\nfor _pid in ALL_PARTNERS:\n    _incs = [i for i in INCIDENTS if _pid in i.affected_partners]\n    _down = sum(i.duration_minutes for i in _incs if i.severity in (Severity.P1, Severity.P2))\n    _up = round((1 - _down / (31*24*60)) * 100, 4)\n    PARTNER_REPORTS.append(PartnerSLAReport(\n        partner_id=_pid, report_month=\"2026-03\", sla_metrics=SLA_METRICS,\n        incidents_count=len(_incs), uptime_pct=_up, credits_owed_usd=0.0))\n\napp = FastAPI(title=\"OCI Robot Cloud — SLA Reporting Service\", version=\"1.0.0\")\n\n@app.get(\"/\", response_class=HTMLResponse)\ndef dashboard():\n    overall_pass = all(m.status == SLAStatus.pass_ for m in SLA_METRICS)\n    banner = \"SLA COMPLIANT\" if overall_pass else \"SLA BREACH\"\n    rows = \"\".join(f\"<tr><td>{m.metric_name}</td><td>{m.sla_target}</td>\"\n                    f\"<td>{m.current_value}</td><td>PASS</td></tr>\" for m in SLA_METRICS)\n    inc_rows = \"\".join(\n        f\"<tr><td>{i.id}</td><td>{i.severity.value}</td><td>{i.title}</td>\"\n        f\"<td>{i.duration_minutes}min</td><td>${i.credits_owed_usd:.2f}</td></tr>\"\n        for i in INCIDENTS\n    )\n    return f\"\"\"<!DOCTYPE html><html><head><title>SLA Dashboard</title></head><body>\n    <h1>OCI Robot Cloud — SLA Dashboard (Port 8080)</h1>\n    <h2 style='color:green'>{banner}</h2>\n    <p>March 2026 | All 7 SLA metrics passing | 3 incidents | $0 credits owed</p>\n    <p>API Uptime: 99.94% | Inference p99: 267ms | Incidents: 3 | Credits: $0.00</p>\n    <h3>SLA Metrics</h3>\n    <table border=1><tr><th>Metric</th><th>Target</th><th>Current</th><th>Status</th></tr>{rows}</table>\n    <h3>Incidents</h3>\n    <table border=1><tr><th>ID</th><th>Sev</th><th>Title</th><th>Duration</th><th>Credits</th></tr>{inc_rows}</table>\n    <p><a href='/docs'>API Docs</a> | <a href='/api/sla/summary'>Summary JSON</a></p>\n    </body></html>\"\"\"\n\n@app.get(\"/health\")\ndef health(): return {\"status\": \"ok\"}\n\n@app.get(\"/api/sla/metrics\")\ndef get_metrics():\n    return {\"metrics\": SLA_METRICS, \"count\": len(SLA_METRICS),\n            \"overall_status\": \"pass\" if all(m.status==SLAStatus.pass_ for m in SLA_METRICS) else \"fail\",\n            \"measurement_period\": MEASUREMENT_PERIOD}\n\n@app.get(\"/api/sla/incidents\")\ndef get_incidents(month: Optional[str]=Query(default=None), severity: Optional[str]=Query(default=None)):\n    results = list(INCIDENTS)\n    if month:\n        try: y, m = map(int, month.split(\"-\"))\n        except: raise HTTPException(400, \"month must be YYYY-MM\")\n        results = [i for i in results if i.start_time.year==y and i.start_time.month==m]\n    if severity:\n        results = [i for i in results if i.severity.value==severity.upper()]\n    return {\"incidents\": results, \"count\": len(results),\n            \"total_credits_owed_usd\": sum(i.credits_owed_usd for i in results)}\n\n@app.get(\"/api/sla/report/{partner_id}\")\ndef get_partner_report(partner_id: str):\n    report = next((r for r in PARTNER_REPORTS if r.partner_id==partner_id), None)\n    if not report: raise HTTPException(404, f\"No report for '{partner_id}'\")\n    return report\n\n@app.get(\"/api/sla/summary\")\ndef get_summary():\n    overall_pass = all(m.status==SLAStatus.pass_ for m in SLA_METRICS)\n    return {\"measurement_period\": MEASUREMENT_PERIOD, \"overall_status\": \"pass\" if overall_pass else \"fail\",\n            \"uptime_pct\": 99.94, \"avg_latency_p99_ms\": 267.0,\n            \"incidents_this_month\": len(INCIDENTS), \"credits_owed_usd\": 0.0,\n            \"metrics_passing\": sum(1 for m in SLA_METRICS if m.status==SLAStatus.pass_)}\n\nif __name__ == \"__main__\":\n    import uvicorn\n    uvicorn.run(\"sla_reporting_service:app\", host=\"0.0.0.0\", port=8080, reload=True)\n