"""\nOCI Robot Cloud — Cost Estimator\nCompute cost estimator and tracker for GR00T training on OCI A100.\nPort 8115\n"""\n\nimport math\nimport hashlib\nimport random\nimport datetime\nimport json\nimport collections\n\ntry:\n    from fastapi import FastAPI\n    from fastapi.responses import HTMLResponse, JSONResponse\n    import uvicorn\n    HAS_FASTAPI = True\nexcept ImportError:\n    HAS_FASTAPI = False\n\n# ---------------------------------------------------------------------------\n# Static data\n# ---------------------------------------------------------------------------\n\nOCI_PRICING = {\n    \"A100_80GB_hr\": 3.06,\n    \"A100_40GB_hr\": 1.28,\n    \"storage_gb_mo\": 0.025,\n}\n\nWORKLOAD_TEMPLATES = [\n    {\"id\": \"bc_finetune_500demo\",  \"gpu\": \"A100_80GB\", \"hours\": 2.5,  \"gpu_count\": 1, \"storage_gb\": 5},\n    {\"id\": \"bc_finetune_1000demo\", \"gpu\": \"A100_80GB\", \"hours\": 5.0,  \"gpu_count\": 1, \"storage_gb\": 10},\n    {\"id\": \"dagger_run_short\",     \"gpu\": \"A100_80GB\", \"hours\": 3.5,  \"gpu_count\": 1, \"storage_gb\": 8},\n    {\"id\": \"dagger_run_full\",      \"gpu\": \"A100_80GB\", \"hours\": 12.0, \"gpu_count\": 1, \"storage_gb\": 20},\n    {\"id\": \"eval_batch_20ep\",      \"gpu\": \"A100_40GB\", \"hours\": 0.8,  \"gpu_count\": 1, \"storage_gb\": 2},\n    {\"id\": \"multi_gpu_finetune\",   \"gpu\": \"A100_80GB\", \"hours\": 4.0,  \"gpu_count\": 2, \"storage_gb\": 15},\n]\n\n# (run_id, category, cost_usd, success_rate or None)\nCOST_HISTORY = [\n    (\"exp_bc_500\",          \"bc\",           2.15,  0.05),\n    (\"exp_bc_1000\",         \"bc\",           4.30,  0.05),\n    (\"exp_dagger_run5\",     \"dagger\",       3.10,  0.05),\n    (\"exp_dagger_run6\",     \"dagger\",       4.80,  0.15),\n    (\"exp_dagger_run9\",     \"dagger\",       8.60,  0.55),\n    (\"exp_dagger_run9_v2\",  \"dagger\",       10.75, 0.71),\n    (\"exp_groot_finetune_v2\",\"full_finetune\",25.80, 0.78),\n    (\"eval_runs_total\",     \"eval\",          6.40,  None),\n]\n\nTOTAL_SPENT = 65.90  # canonical total\n\n# ---------------------------------------------------------------------------\n# Business logic\n# ---------------------------------------------------------------------------\n\ndef estimate_cost(template_id: str, count: int = 1) -> dict:\n    tpl = next((t for t in WORKLOAD_TEMPLATES if t[\"id\"] == template_id), None)\n    if tpl is None:\n        return {\"error\": f\"Template '{template_id}' not found\"}\n\n    gpu_key = tpl[\"gpu\"] + \"_hr\"\n    gpu_rate = OCI_PRICING.get(gpu_key, 0.0)\n    gpu_count = tpl.get(\"gpu_count\", 1)\n    hours = tpl[\"hours\"]\n    storage_gb = tpl[\"storage_gb\"]\n\n    gpu_cost = round(gpu_rate * hours * gpu_count * count, 4)\n    # storage billed per month; approximate 1 month\n    storage_cost = round(OCI_PRICING[\"storage_gb_mo\"] * storage_gb * count, 4)\n    total = round(gpu_cost + storage_cost, 4)\n\n    return {\n        \"template_id\": template_id,\n        \"count\": count,\n        \"gpu\": tpl[\"gpu\"],\n        \"gpu_count\": gpu_count,\n        \"hours\": hours,\n        \"storage_gb\": storage_gb,\n        \"gpu_rate_per_hr\": gpu_rate,\n        \"gpu_cost_usd\": gpu_cost,\n        \"storage_cost_usd\": storage_cost,\n        \"total_cost_usd\": total,\n    }\n\n\ndef cost_summary() -> dict:\n    by_category = collections.defaultdict(float)\n    max_sr = 0.0\n    for run_id, cat, cost, sr in COST_HISTORY:\n        by_category[cat] += cost\n        if sr is not None and sr > max_sr:\n            max_sr = sr\n\n    by_category = {k: round(v, 2) for k, v in by_category.items()}\n    cost_per_sr = round(TOTAL_SPENT / max_sr, 2) if max_sr > 0 else None\n\n    # best value: highest SR per dollar (excluding eval_runs_total which has no SR)\n    best_value_run = None\n    best_ratio = -1.0\n    for run_id, cat, cost, sr in COST_HISTORY:\n        if sr is not None and cost > 0:\n            ratio = sr / cost\n            if ratio > best_ratio:\n                best_ratio = ratio\n                best_value_run = run_id\n\n    return {\n        \"total_spent_usd\": TOTAL_SPENT,\n        \"by_category\": by_category,\n        \"max_success_rate\": max_sr,\n        \"cost_per_sr_point_usd\": cost_per_sr,\n        \"best_value_run\": best_value_run,\n        \"run_count\": len(COST_HISTORY),\n    }\n\n# ---------------------------------------------------------------------------\n# SVG: cumulative cost line chart\n# ---------------------------------------------------------------------------\n\ndef build_cumulative_cost_svg() -> str:\n    W, H = 700, 160\n    ML, MR, MT, MB = 55, 20, 20, 35\n\n    runs = [r for r in COST_HISTORY]\n    n = len(runs)\n    cumulative = []\n    running = 0.0\n    for r in runs:\n        running += r[2]\n        cumulative.append(round(running, 2))\n\n    max_cost = cumulative[-1]\n    plot_w = W - ML - MR\n    plot_h = H - MT - MB\n\n    def px(i, c):\n        x = ML + (i / (n - 1)) * plot_w if n > 1 else ML\n        y = MT + plot_h - (c / max_cost) * plot_h\n        return round(x, 1), round(y, 1)\n\n    # area path\n    pts = [px(i, c) for i, c in enumerate(cumulative)]\n    area_d = f\"M {pts[0][0]} {MT + plot_h}\"\n    for p in pts:\n        area_d += f\" L {p[0]} {p[1]}\"\n    area_d += f\" L {pts[-1][0]} {MT + plot_h} Z\"\n\n    line_d = f\"M {pts[0][0]} {pts[0][1]}\"\n    for p in pts[1:]:\n        line_d += f\" L {p[0]} {p[1]}\"\n\n    # y axis ticks\n    y_ticks_vals = [0, 20, 40, 60, max_cost]\n    y_ticks_svg = \"\"\n    for v in y_ticks_vals:\n        py = MT + plot_h - (v / max_cost) * plot_h\n        y_ticks_svg += (\n            f'<line x1="{ML - 4}" y1="{py:.1f}" x2="{ML}" y2="{py:.1f}" '\n            f'stroke="#475569" stroke-width="1"/>'\n            f'<text x="{ML - 7}" y="{py + 3:.1f}" fill="#94a3b8" '\n            f'font-size="9" text-anchor="end">${v:.0f}</text>'\n        )\n\n    # x axis labels (run names shortened)\n    x_labels_svg = \"\"\n    for i, r in enumerate(runs):\n        x, _ = px(i, 0)\n        short = r[0].replace(\"exp_\", \"\").replace(\"_\", \" \")\n        x_labels_svg += (\n            f'<text x="{x}" y="{MT + plot_h + 14}" fill="#94a3b8" '\n            f'font-size="7" text-anchor="middle">{short}</text>'\n        )\n\n    dots_svg = \"\"\n    for i, (c, r) in enumerate(zip(cumulative, runs)):\n        px_x, px_y = px(i, c)\n        dots_svg += (\n            f'<circle cx="{px_x}" cy="{px_y}" r="3" fill="#C74634"/>'\n            f'<title>{r[0]}: ${c:.2f}</title>'\n        )\n\n    svg = (\n        f'<svg xmlns="http://www.w3.org/2000/svg" width="{W}" height="{H}" '\n        f'style="background:#1e293b;border-radius:8px;">'\n        # axes\n        f'<line x1="{ML}" y1="{MT}" x2="{ML}" y2="{MT + plot_h}" stroke="#475569" stroke-width="1"/>'\n        f'<line x1="{ML}" y1="{MT + plot_h}" x2="{ML + plot_w}" y2="{MT + plot_h}" stroke="#475569" stroke-width="1"/>'\n        + y_ticks_svg\n        + x_labels_svg\n        + f'<path d="{area_d}" fill="#C74634" fill-opacity="0.25"/>'\n        + f'<path d="{line_d}" fill="none" stroke="#C74634" stroke-width="2"/>'\n        + dots_svg\n        + f'<text x="{ML + plot_w / 2}" y="{H - 2}" fill="#94a3b8" font-size="10" text-anchor="middle">Run Index</text>'\n        f'<text x="12" y="{MT + plot_h / 2}" fill="#94a3b8" font-size="10" text-anchor="middle" '\n        f'transform="rotate(-90,12,{MT + plot_h / 2})">Cumulative $</text>'\n        f'</svg>'\n    )\n    return svg\n\n# ---------------------------------------------------------------------------\n# SVG: cost vs SR scatter\n# ---------------------------------------------------------------------------\n\ndef build_cost_sr_scatter_svg() -> str:\n    W, H = 700, 160\n    ML, MR, MT, MB = 55, 20, 20, 35\n\n    sr_runs = [(r[0], r[2], r[3]) for r in COST_HISTORY if r[3] is not None]\n    if not sr_runs:\n        return '<svg width=\"700\" height=\"160\"><text x=\"350\" y=\"80\" fill=\"#94a3b8\" text-anchor=\"middle\">No SR data</text></svg>'\n\n    max_cost = max(r[1] for r in sr_runs)\n    plot_w = W - ML - MR\n    plot_h = H - MT - MB\n\n    def to_px(cost, sr):\n        x = ML + (cost / max_cost) * plot_w\n        y = MT + plot_h - sr * plot_h\n        return round(x, 1), round(y, 1)\n\n    # axes\n    x_ticks = [0, 5, 10, 15, 20, 25, round(max_cost)]\n    x_ticks_svg = \"\"\n    for v in x_ticks:\n        px = ML + (v / max_cost) * plot_w\n        x_ticks_svg += (\n            f'<line x1="{px:.1f}" y1="{MT + plot_h}" x2="{px:.1f}" '\n            f'y2="{MT + plot_h + 5}" stroke="#475569" stroke-width="1"/>'\n            f'<text x="{px:.1f}" y="{MT + plot_h + 15}" fill="#94a3b8" '\n            f'font-size="9" text-anchor="middle">${v}</text>'\n        )\n\n    y_ticks_vals = [0.0, 0.25, 0.5, 0.75, 1.0]\n    y_ticks_svg = \"\"\n    for v in y_ticks_vals:\n        py = MT + plot_h - v * plot_h\n        y_ticks_svg += (\n            f'<line x1="{ML - 4}" y1="{py:.1f}" x2="{ML}" y2="{py:.1f}" '\n            f'stroke="#475569" stroke-width="1"/>'\n            f'<text x="{ML - 7}" y="{py + 3:.1f}" fill="#94a3b8" '\n            f'font-size="9" text-anchor="end">{v:.0%}</text>'\n        )\n\n    dots_svg = \"\"\n    for run_id, cost, sr in sr_runs:\n        dx, dy = to_px(cost, sr)\n        short = run_id.replace(\"exp_\", \"\").replace(\"_\", \" \")\n        dots_svg += (\n            f'<circle cx="{dx}" cy="{dy}" r="5" fill="#38bdf8" fill-opacity="0.8"/>'\n            f'<text x="{dx}" y="{dy - 8}" fill="#e2e8f0" font-size="7" text-anchor="middle">{short}</text>'\n        )\n\n    svg = (\n        f'<svg xmlns="http://www.w3.org/2000/svg" width="{W}" height="{H}" '\n        f'style="background:#1e293b;border-radius:8px;">'\n        f'<line x1="{ML}" y1="{MT}" x2="{ML}" y2="{MT + plot_h}" stroke="#475569" stroke-width="1"/>'\n        f'<line x1="{ML}" y1="{MT + plot_h}" x2="{ML + plot_w}" y2="{MT + plot_h}" stroke="#475569" stroke-width="1"/>'\n        + x_ticks_svg\n        + y_ticks_svg\n        + dots_svg\n        + f'<text x="{ML + plot_w / 2}" y="{H - 2}" fill="#94a3b8" font-size="10" text-anchor="middle">Cost (USD)</text>'\n        f'<text x="12" y="{MT + plot_h / 2}" fill="#94a3b8" font-size="10" text-anchor="middle" '\n        f'transform="rotate(-90,12,{MT + plot_h / 2})">Success Rate</text>'\n        f'</svg>'\n    )\n    return svg\n\n# ---------------------------------------------------------------------------\n# HTML builder\n# ---------------------------------------------------------------------------\n\ndef build_html() -> str:\n    summary = cost_summary()\n    cum_svg = build_cumulative_cost_svg()\n    scatter_svg = build_cost_sr_scatter_svg()\n\n    # Estimator form rows\n    template_options = \"\".join(\n        f'<option value=\"{t[\"id\"]}\">{t[\"id\"]} ({t[\"gpu\"]}, {t[\"hours\"]}h x{t.get(\"gpu_count\",1)} GPU, {t[\"storage_gb\"]}GB)</option>'\n        for t in WORKLOAD_TEMPLATES\n    )\n\n    # History table rows\n    history_rows = \"\"\n    running = 0.0\n    for run_id, cat, cost, sr in COST_HISTORY:\n        running += cost\n        sr_display = f\"{sr:.0%}\" if sr is not None else \"—\"\n        history_rows += f\"\"\"\n        <tr>\n          <td style=\"color:#38bdf8;font-family:monospace;\">{run_id}</td>\n          <td>{cat}</td>\n          <td>${cost:.2f}</td>\n          <td>{sr_display}</td>\n          <td>${running:.2f}</td>\n        </tr>\n        \"\"\"\n\n    html = f\"\"\"<!DOCTYPE html>\n<html lang=\"en\">\n<head>\n  <meta charset=\"UTF-8\"/>\n  <title>OCI Robot Cloud — Cost Estimator</title>\n  <style>\n    * {{ box-sizing: border-box; margin: 0; padding: 0; }}\n    body {{ background: #0f172a; color: #e2e8f0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; padding: 24px; }}\n    h1 {{ color: #C74634; font-size: 24px; margin-bottom: 4px; }}\n    h2 {{ color: #C74634; font-size: 16px; margin: 24px 0 12px; }}\n    .subtitle {{ color: #94a3b8; font-size: 13px; margin-bottom: 24px; }}\n    .card {{ background: #1e293b; border-radius: 8px; padding: 16px; }}\n    .card-label {{ color: #94a3b8; font-size: 11px; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 6px; }}\n    .card-value {{ color: #38bdf8; font-size: 26px; font-weight: 700; }}\n    .card-sub {{ color: #64748b; font-size: 11px; margin-top: 4px; }}\n    table {{ width: 100%; border-collapse: collapse; background: #1e293b; border-radius: 8px; overflow: hidden; }}\n    th {{ background: #0f172a; color: #94a3b8; font-size: 11px; text-transform: uppercase; letter-spacing: 0.05em; padding: 10px 12px; text-align: left; }}\n    td {{ padding: 10px 12px; border-top: 1px solid #0f172a; font-size: 13px; vertical-align: middle; }}\n    tr:hover td {{ background: #243044; }}\n    .chart-wrap {{ background: #1e293b; border-radius: 8px; padding: 16px; margin-bottom: 24px; }}\n    .chart-title {{ color: #94a3b8; font-size: 12px; margin-bottom: 10px; }}\n    .form-card {{ background: #1e293b; border-radius: 8px; padding: 20px; margin-bottom: 24px; }}\n    .form-row {{ display: flex; gap: 12px; align-items: flex-end; flex-wrap: wrap; }}\n    label {{ color: #94a3b8; font-size: 12px; display: block; margin-bottom: 4px; }}\n    select, input {{ background: #0f172a; color: #e2e8f0; border: 1px solid #334155; border-radius: 6px; padding: 8px 10px; font-size: 13px; }}\n    button {{ background: #C74634; color: #fff; border: none; border-radius: 6px; padding: 9px 20px; font-size: 13px; cursor: pointer; font-weight: 600; }}\n    button:hover {{ background: #a53828; }}\n    #estimate-result {{ margin-top: 14px; background: #0f172a; border-radius: 6px; padding: 12px; font-size: 13px; color: #38bdf8; white-space: pre; font-family: monospace; display: none; }}\n    footer {{ margin-top: 32px; color: #475569; font-size: 11px; text-align: center; }}\n  </style>\n</head>\n<body>\n  <h1>Cost Estimator</h1>\n  <div class=\"subtitle\">GR00T Training Cost Tracker &amp; Estimator — OCI A100 Fleet</div>\n\n  <div style=\"display:flex;gap:16px;flex-wrap:wrap;margin-bottom:24px;\">\n    <div class=\"card\" style=\"flex:1;min-width:160px;\">\n      <div class=\"card-label\">Total Spent</div>\n      <div class=\"card-value\">${summary['total_spent_usd']:.2f}</div>\n      <div class=\"card-sub\">{summary['run_count']} training runs</div>\n    </div>\n    <div class=\"card\" style=\"flex:1;min-width:160px;\">\n      <div class=\"card-label\">Cost / SR Point</div>\n      <div class=\"card-value\">${summary['cost_per_sr_point_usd']:.1f}</div>\n      <div class=\"card-sub\">at max SR = {summary['max_success_rate']:.0%}</div>\n    </div>\n    <div class=\"card\" style=\"flex:1;min-width:160px;\">\n      <div class=\"card-label\">Best Value Run</div>\n      <div class=\"card-value\" style=\"font-size:16px;word-break:break-all;\">{summary['best_value_run']}</div>\n      <div class=\"card-sub\">highest SR per $ spent</div>\n    </div>\n  </div>\n\n  <h2>Cumulative Cost Over Runs</h2>\n  <div class=\"chart-wrap\">\n    <div class=\"chart-title\">Cumulative cost (USD) across all training runs — Oracle red area</div>\n    {cum_svg}\n  </div>\n\n  <h2>Cost vs Success Rate</h2>\n  <div class=\"chart-wrap\">\n    <div class=\"chart-title\">Each dot = one experiment; x = cost (USD), y = success rate</div>\n    {scatter_svg}\n  </div>\n\n  <h2>Cost Estimator</h2>\n  <div class=\"form-card\">\n    <div class=\"form-row\">\n      <div>\n        <label for=\"tpl-select\">Workload Template</label>\n        <select id=\"tpl-select\">\n          {template_options}\n        </select>\n      </div>\n      <div>\n        <label for=\"count-input\">Count (runs)</label>\n        <input id=\"count-input\" type=\"number\" min=\"1\" value=\"1\" style=\"width:80px;\"/>\n      </div>\n      <button onclick=\"runEstimate()\">Estimate Cost</button>\n    </div>\n    <pre id=\"estimate-result\"></pre>\n  </div>\n\n  <h2>Cost History</h2>\n  <table>\n    <thead>\n      <tr><th>Run ID</th><th>Category</th><th>Cost</th><th>Success Rate</th><th>Cumulative</th></tr>\n    </thead>\n    <tbody>\n      {history_rows}\n    </tbody>\n  </table>\n\n  <footer>Oracle Confidential | OCI Robot Cloud Cost Estimator | Port 8115</footer>\n\n  <script>\n    async function runEstimate() {{\n      const tpl = document.getElementById('tpl-select').value;\n      const count = parseInt(document.getElementById('count-input').value) || 1;\n      const el = document.getElementById('estimate-result');\n      el.style.display = 'block';\n      el.textContent = 'Calculating...';\n      try {{\n        const resp = await fetch(`/estimate/${{encodeURIComponent(tpl)}}?count=${{count}}`);\n        const data = await resp.json();\n        if (data.error) {{\n          el.textContent = 'Error: ' + data.error;\n        }} else {{\n          el.textContent = JSON.stringify(data, null, 2);\n        }}\n      }} catch(e) {{\n        el.textContent = 'Could not reach API (static preview mode).';\n      }}\n    }}\n  </script>\n</body>\n</html>\"\"\"\n    return html\n\n# ---------------------------------------------------------------------------\n# FastAPI app\n# ---------------------------------------------------------------------------\n\nif HAS_FASTAPI:\n    app = FastAPI(title=\"OCI Robot Cloud — Cost Estimator\", version=\"1.0.0\")\n\n    @app.get(\"/\", response_class=HTMLResponse)\n    def root():\n        return build_html()\n\n    @app.get(\"/estimate/{template_id}\")\n    def get_estimate(template_id: str, count: int = 1):\n        result = estimate_cost(template_id, count)\n        if \"error\" in result:\n            return JSONResponse(status_code=404, content=result)\n        return JSONResponse(content=result)\n\n    @app.get(\"/history\")\n    def get_history():\n        rows = []\n        running = 0.0\n        for run_id, cat, cost, sr in COST_HISTORY:\n            running += cost\n            rows.append({\n                \"run_id\": run_id,\n                \"category\": cat,\n                \"cost_usd\": cost,\n                \"success_rate\": sr,\n                \"cumulative_usd\": round(running, 2),\n            })\n        return JSONResponse(content=rows)\n\n    @app.get(\"/summary\")\n    def get_summary():\n        return JSONResponse(content=cost_summary())\n\n    @app.get(\"/health\")\n    def health():\n        return JSONResponse(content={\n            \"status\": \"ok\",\n            \"service\": \"cost_estimator\",\n            \"port\": 8115,\n            \"timestamp\": datetime.datetime.utcnow().isoformat() + \"Z\",\n            \"total_spent_usd\": TOTAL_SPENT,\n        })\n\n# ---------------------------------------------------------------------------\n# CLI fallback\n# ---------------------------------------------------------------------------\n\ndef main():\n    html = build_html()\n    out_path = \"/tmp/cost_estimator.html\"\n    with open(out_path, \"w\", encoding=\"utf-8\") as f:\n        f.write(html)\n    print(f\"Saved to {out_path}\")\n\n    if HAS_FASTAPI:\n        uvicorn.run(app, host=\"0.0.0.0\", port=8115)\n    else:\n        print(\"fastapi/uvicorn not installed. HTML saved to /tmp/cost_estimator.html\")\n\nif __name__ == \"__main__\":\n    main()\n